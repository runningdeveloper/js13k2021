{
  "name": "js13k2021",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "snowpack dev",
    "clean": "rm -rf ./build && rm ./out.zip || true",
    "zip": "npm run build && zip -r out.zip build",
    "build": "npm run clean && snowpack build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "kontra": "^7.1.3"
  },
  "devDependencies": {
    "snowpack": "^3.8.6",
    "snowpack-plugin-minify-html": "^0.1.1",
    "snowpack-plugin-terser": "^0.1.0",
    "terser": "^5.7.1"
  }
}
